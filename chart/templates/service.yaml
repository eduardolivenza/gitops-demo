apiVersion: v1
kind: Service
metadata:
  name: {{ include "wingmanhelm.fullname" . }}
  labels:
    {{- include "wingmanhelm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wingmanhelm.selectorLabels" . | nindent 4 }}
